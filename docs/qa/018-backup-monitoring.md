# QA — SPEC 018 (gateway half)

CRM backup loop: **goso-crm** `docs/qa/018-backup-monitoring.md` (restore count 7 = 7).

This repo: `GET /api/metrics` aliases `GET /api/stats`. Unit test `TestHandleStatsAndMetrics` hits the alias.
