# QA — SPEC 014 Connector Architecture

Date: 2026-08-22
Branch: `admatrixmdp/spec014`
Base: `main` @ `e11755c`

Clean-room implementation. No copy/import/vendor from `Zalo CRM/`, `zalocrm-mcp/`, `goso-crm`, or authors `minhhaiphan` / `locphamnguyen`. ZaloCRM is a remote connector (MCP and/or HTTP client to `GOSOCRM_API_URL`, default `http://127.0.0.1:8089`). Health = GET `/healthz` or `/readyz`.

## Commands

```
make verify
cd control-plane && npm run typecheck && npm run build
./scripts/e2e-connector.sh
grep -r minhhaiphan gateway || true
```

## AC checklist

| AC | Result | Evidence |
|----|--------|----------|
| AC-01 `gateway/internal/connector` interface + Registry; unit tests, no network | PASS | `Connector` in `connector.go`; `Registry` Register/Lookup/List; disabled Invoke → `ErrUnavailable` (`connector_unavailable`). `go test ./gateway/internal/connector -run Registry` |
| AC-02 MCP streamable-http + stdio; fake MCP in-process | PASS | `mcp.go` JSON-RPC HTTP + Content-Length stdio. `ServeFakeMCP` + `StartFakeMCPStdio`. `go test ./gateway/internal/connector -run MCP` |
| AC-03 HTTP REST + Bearer; manifest URL or inline | PASS | `http.go`. Tests: `TestHTTPTransport_ManifestURLAndInvoke`, `TestHTTPTransport_InlineManifest` |
| AC-04 Approval Gate; `POST /api/approvals/{id}/decision` | PASS | `gateway/internal/approval`. Tool Layer does **not** Invoke when `requires_approval`. Returns `pending_approval` + `approval_id`. Relay to remote owner; goso does not execute Zalo send. `go test ./gateway/internal/approval` + `TestConnector_CRUDAndApproval` |
| AC-05 `POST/GET /api/connectors`, `POST /api/agents/{id}/connectors`; Control Plane list + health + assign | PASS | `handlers_connector.go`; `control-plane/src/pages/Connectors.tsx`. Existing `/healthz`, `/api/agents`, `/api/sessions`, `/api/chat` still green |
| AC-06 Agent runtime: tool list from connectors; Message `role=tool`; trace connector + latency | PASS | `gateway/internal/agent`. `go test ./gateway/internal/agent -run Tools` |
| AC-07 `make verify` + e2e fake CRM (`contact_search`) + POS; sensitive → pending_approval | PASS | `make verify` OK. `./scripts/e2e-connector.sh` OK. Fixtures in `scripts/fixtures/`. No live Zalo / AGPL binaries |
| AC-08 No AGPL copy; GOSO header on new Go files; `zalocrm` only as connector name | PASS | `grep -r minhhaiphan gateway` empty. `zalocrm` only string config/tests. Header: `// Copyright (c) 2026 MQ Global — GOSO Gateway. Clean-room implementation.` |
| AC-09 EventStore + `GET /api/events` + Control Plane Events | PASS | `gateway/internal/eventstore`; kinds attempt/success/error/connector_unavailable/pending_approval/human_feedback. Credentials redacted. `control-plane/src/pages/Events.tsx` |

## T01–T12

| Task | Status |
|------|--------|
| T01 Registry | done |
| T02 Manifest | done |
| T03 MCP | done |
| T04 HTTP | done |
| T05 Approval | done |
| T06 CRUD API | done |
| T07 Tool layer | done |
| T08 Health/retry | done |
| T09 Control Plane Connectors | done |
| T10 e2e | done |
| T11 EventStore | done |
| T12 QA + verify | done |

## Notes

- Timeout/retry: `WithRetry` — offline/timeout → `connector_unavailable`, does not hang (`TestHealth_TimeoutUnavailableNoHang`).
- Control Plane tabs: Agents, Sessions, Chat, Connectors, Events (Agents/Sessions/Chat kept).
- `npm run typecheck` and `npm run build` in `control-plane/` succeeded.
- Merged to `main` @ `12eb09c` (coordinator, 2026-08-22). Branch `admatrixmdp/spec014` @ `bff48b2`.
