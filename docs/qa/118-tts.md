# QA — SPEC 118 TTS

Date: 2026-08-30. Clean-room React/Go. No ZaloCRM / goclaw-source copy. No banned author ids. Demos `:8082` `:8091` `:18080` `:18791` — not bound or killed. `:3000` belongs to the coordinator. Do not paste goclaw Go. Do not merge. No live vendor tokens. No secrets in git/QA.

## GoClaw behavior (READ-ONLY cite — paths only)

Source tree: `/Users/mqglobal/Documents/goclaw/goclaw-source` (CC-BY-NC). Behavior was read from the 090 audit notes, never from `.go` sources, never vendored. Do not paste goclaw Go.

| Behavior | Cite |
|----------|------|
| TTS: provider selector, provider settings, expandable advanced settings; refresh, select provider, test connection, save; auto-apply/reply mode and limits; API-key fields for paid providers; require write-only contract | `docs/qa/090-goclaw-sidebar-ux.md` TTS |

goso mapping (self-written): live tab `tts` in [App.tsx](../../control-plane/src/App.tsx) renders [TTSPage](../../control-plane/src/pages/TTSPage.tsx). Status is `GET /api/tts` (`/v1/tts` alias) with `provider`, `enabled`, `configured`, `key_set`, `env_owned`, `source`, voice/model/language/region/endpoint, `auto_apply`, `max_chars`, `timeout_ms`. Save is `PUT /api/tts`. Test is `POST /api/tts/test`. Clear is `POST /api/tts/clear` `{confirm}` matching provider or `tts`. GET never returns `api_key` / authorization. Empty PUT omits/keeps the stored key. Env overlay `GOSO_TTS_*` refuses PUT secrets (409). View-token GET 200 / PUT-POST 403. Issued `read` GET 200 / PUT 403; `write` may save. Test failures redact authorization details. Mutations append SPEC 110 audit rows (`entity=tts`) without tokens. Builtin `tts` invoke stays fail-closed without a test double (086/074).

Out of scope: copying GoClaw chrome. Live vendor tokens. Backup (117) already merged. Re-doing S3/backup.

## What changed

- Live nav tab + page. Provider select, provider-specific fields, test/save, loading / empty / error / disabled / not-configured.
- Auto-apply/reply policy, character limit, timeout. Write-only keys; GET boolean status only. Test failures redact authorization.
- i18n vi+en. CP typecheck. Tests. agpl 0. `docs/qa/118-tts.md`.

## Commands

```
cd control-plane && npm run typecheck && npm test
go test ./gateway/internal/tts ./gateway/internal/auth ./gateway/internal/httpapi ./gateway/internal/serve -count=1
GOSO_ROOT=$PWD /Users/mqglobal/Documents/goclaw-binary/goso-crm/scripts/agpl-check.sh
./scripts/agpl-check-docs.sh
```

Do not bind or kill demo ports `:8082` `:8091` `:18080` `:18791`. Do not merge. Coordinator owns `:3000`.

## Proof

- `npm run typecheck` exit 0. `npm test` including `asPublicStatus` dropping `api_key` rows, `ttsWriteBody` omitting blank keys, `formatTTSTest` / `parseTTSTestError` redacting Bearer/sk.
- `go test` tts: GET/public omits key; empty PUT keeps key; env-owned PUT key/clear 409; env-owned PUT endpoint is ignored so Test never sends the env key to a memory URL; local test for `edge`; missing key and disabled; HTTP 401 body with authorization is redacted to `unauthorized`; probe uses CheckURL/GuardClient and requires 2xx. httpapi: GET `/v1` alias; PUT never echoes key; test 400 redacts; confirm clear; view-token GET 200 / PUT-POST 403; issued `read` GET 200 / PUT 403, `write` can save. auth/serve: view GET `/api/tts` 200, PUT/POST 403.
- Page copy: “TTS is not configured.” / “TTS chưa cấu hình.” Disabled empty state. Password input empty on load. Distinct from Functions (096) and Backup (117).
- `agpl-check` and `agpl-check-docs` exit 0.

## Non-goals

Copying GoClaw chrome. Live vendor tokens. Binding/killing demo ports. Merge. Backup (117). Paid TTS synthesis / builtin `tts` invoke (still fail-closed without a test double).
