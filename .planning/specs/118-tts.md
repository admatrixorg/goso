# SPEC 118 — TTS

> After 117. Clean-room React/Go. Do **not** copy GoClaw/ZaloCRM. i18n vi+en. Do not bind/kill `:8082` `:8091`. `:3000` may restart. No invent live vendor tokens.

Queue: `.planning/specs/090-sidebar-ux-queue.md` row 118. Audit: `docs/qa/090-goclaw-sidebar-ux.md` TTS. **THIẾU**.

## Goal

Add TTS provider selection, provider-specific test/save, auto-apply/reply policy, limits, timeout, and disabled/not-configured states as an isolated capability page or a well-bounded Functions subpage. Provider keys must use the common write-only contract with boolean status on GET, and test failures must redact authorization details.

## AC

- [ ] Live nav tab or Functions subpage. Provider select, test/save, loading/empty/error/disabled/not-configured.
- [ ] Auto-apply/reply policy, limits, timeout. Write-only keys; GET boolean status only. Test failures redact authorization.
- [ ] i18n vi+en. CP typecheck. Tests. agpl 0. `docs/qa/118-tts.md`.

## Out of scope

Copying GoClaw chrome. Live vendor tokens. Backup (117) already merged.
