# SPEC 019 — i18n VI/EN (control-plane)

> LOCKED: 2026-08-26. Lot 2. Default **vi**. Pair goso-crm SPEC 019.

## Goal

`control-plane/src/i18n/{vi,en}.ts` + `t(key)` + language toggle in chrome (persists `localStorage.goso_lang`). Replace user-visible string literals in pages/nav. Keep DEMO gate (SPEC 017).

## Own

`control-plane/src/i18n/`, `App.tsx` toggle, pages labels. Do not change gateway Go.

## AC

- [ ] AC-01 Default vi nav “Tổng quan”.
- [ ] AC-02 Switch to EN updates nav/pages without reload of data.
- [ ] AC-03 `npm run typecheck` + `VITE_DEMO_MODE=false npm run build`.
- [ ] AC-04 Non-demo still hides DEMO tabs.

## Non-goals

13 settings, marketing API, desktop (SPEC 024).
